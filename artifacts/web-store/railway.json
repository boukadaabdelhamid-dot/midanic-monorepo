{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --filter @workspace/web-store... && pnpm --filter @workspace/web-store run build"
  },
  "deploy": {
    "startCommand": "npx serve -s /app/artifacts/web-store/dist/public -l $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
